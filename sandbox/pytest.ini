[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: needs a real Chromium install and/or real network access (not just loopback). Skip with -m "not integration" in network-restricted environments.
testpaths = tests
